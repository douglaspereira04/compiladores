def def
int int
float float
string string
break break
print print
read read
return return
if if
else else
for for
new new
leqt <=
lt <
geqt >=
gt >
eq ==
attr =
diff !=
op \(
cp \)
ob {
cb }
osb \[
csb \]
plus \+
minus -
times \*
divide /
percent %
comma ,
semicolon ;
string_constant ".*"
float_constant [\+-]?[0-9]+\.[0-9]+(E[\+-]?[0-9]+(\.[0-9]+)?)?
int_constant [\+-]?[0-9]+(E[\+-]?[0-9]+)?
ident ([a-z]|[A-Z])([a-z]|[A-Z]|[0-9])*
ignore  	
IDENT ident
